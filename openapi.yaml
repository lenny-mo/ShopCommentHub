# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: CommentService API
    description: 定义商品评价服务
    version: 0.0.1
paths:
    /product-review/v1/add-comment:
        post:
            tags:
                - CommentService
            description: |-
                消费者流程
                 对商品SKUID=10发表评价
            operationId: CommentService_AddComment
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CommentRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CommentResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /product-review/v1/add-product:
        post:
            tags:
                - CommentService
            description: |-
                商家流程
                 发布商品SKUID=10
            operationId: CommentService_AddProduct
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MerchantAddProductRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MerchantAddProductResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /product-review/v1/add-product-reply:
        post:
            tags:
                - CommentService
            description: 对商品SKUID=10的其中一条评价发表回复
            operationId: CommentService_AddProductReply
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MerchantReplyRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MerchantReplyResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /product-review/v1/add-reply:
        post:
            tags:
                - CommentService
            description: 对商品SKUID=10的其中的一条评价发表回复
            operationId: CommentService_AddReply
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ReplyRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ReplyResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /product-review/v1/get-comments/{skuId}:
        get:
            tags:
                - CommentService
            description: |-
                common 读模型
                 查看商品SKUID=10的所有评价
            operationId: CommentService_GetComments
            parameters:
                - name: skuId
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetCommentsResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        Comment:
            type: object
            properties:
                commentId:
                    type: integer
                    format: int32
                consumerId:
                    type: string
                commentContent:
                    type: string
            description: 评价消息定义
        CommentRequest:
            type: object
            properties:
                consumerId:
                    type: string
                lastVersion:
                    type: string
                skuId:
                    type: string
                commentContent:
                    type: string
            description: |-
                消费者流程
                 对商品SKUID=10发表评价
        CommentResponse:
            type: object
            properties:
                success:
                    type: boolean
                message:
                    type: string
        GetCommentsResponse:
            type: object
            properties:
                commentList:
                    type: array
                    items:
                        $ref: '#/components/schemas/Comment'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        MerchantAddProductRequest:
            type: object
            properties:
                merchantId:
                    type: string
                product:
                    $ref: '#/components/schemas/Product'
            description: |-
                商家流程
                 发布商品SKUID=10
        MerchantAddProductResponse:
            type: object
            properties:
                success:
                    type: boolean
                message:
                    type: string
        MerchantReplyRequest:
            type: object
            properties:
                merchantId:
                    type: string
                skuId:
                    type: string
                lastCommentId:
                    type: string
                replyContent:
                    type: string
                lastVersion:
                    type: string
            description: 对商品SKUID=10的其中一条评价发表回复
        MerchantReplyResponse:
            type: object
            properties:
                success:
                    type: boolean
                message:
                    type: string
        Product:
            type: object
            properties:
                skuId:
                    type: string
                title:
                    type: string
            description: 商品消息定义
        ReplyRequest:
            type: object
            properties:
                consumerId:
                    type: string
                skuId:
                    type: string
                lastCommentId:
                    type: string
                replyContent:
                    type: string
                lastVersion:
                    type: string
            description: 对商品SKUID=10的其中的一条评价发表回复
        ReplyResponse:
            type: object
            properties:
                success:
                    type: boolean
                message:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: CommentService
